# amplitudevoice.com

Website for www.amplitudevoice.com, served by GitHub Pages from this repository.

Plain HTML and CSS — no build step. Editing `index.html` or `styles.css` and
pushing to `main` publishes the change.

Kept intentionally separate from the personal site: nothing is shared between
the two repositories.
